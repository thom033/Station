docker start 15c4f4234cdb

docker logs -f 15c4f4234cdb


docker exec -it oracle-xe-11g bash


java -version


update-alternatives --list java


export JAVA_HOME=/usr/lib/jvm/java-8-openjdk-amd64
export PATH=$JAVA_HOME/bin:$PATH

echo $JAVA_HOME
java -version


su - oracle
sqlplus / as sysdba

CREATE USER gallois IDENTIFIED BY gallois;


GRANT DBA TO gallois;

docker cp "/home/ninah/cluster/Station/base.dmp" oracle-xe-11g:/tmp/base.dmp
imp gallois/gallois File= /tmp/base.dmp full= y 


iif want to delete
SELECT sid, serial#, username, status, machine, program
FROM V$SESSION
WHERE username = 'GALLOIS'; -- Case-sensitive!


ALTER SYSTEM KILL SESSION '10,12923';
ALTER SYSTEM KILL SESSION '51,12989';
ALTER SYSTEM KILL SESSION '139,105';

Mandefa Oracle
	docker start oracle-xe-11g
	docker exec -it oracle-xe-11g bash
	sqlplus system/oracle@XE

	
docker run -d --name oracle-xe-11g -p 1521:1521 -p 8080:8080 oracleinanutshell/oracle-xe-11g